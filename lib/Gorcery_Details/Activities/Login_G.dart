import 'package:flutter/material.dart';

class Login_G extends StatelessWidget
{ const Login_G({super.key});

  @override
  Widget build(BuildContext context) {
   return Scaffold(
     appBar: AppBar(
       title: Text('login page'),
     ),
     body: Text('login page'),
   );
  }


}